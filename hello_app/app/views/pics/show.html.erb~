<% provide(:title, @pic.id) %>
<div class="row">
  <aside class="col-md-4">
    <section class="pic_info">
      <h1>
	<%= image_tag(@pic.picInfo) %>
	<%= @pic.animalType %>
	<%= @pic.likes %>
      </h1>
    </section>
  </aside>
</div>
